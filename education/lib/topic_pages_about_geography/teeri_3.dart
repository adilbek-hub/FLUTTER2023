import 'package:flutter/material.dart';

class Teeri3 extends StatelessWidget {
  const Teeri3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('Teeri3'),
      ),
    );
  }
}
