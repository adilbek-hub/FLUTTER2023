import 'package:flutter/material.dart';

class TemaH2 extends StatelessWidget {
  const TemaH2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('TemaH2'),
      ),
    );
  }
}
