import 'package:flutter/material.dart';

class TemaH3 extends StatelessWidget {
  const TemaH3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('TemaH3'),
      ),
    );
  }
}
